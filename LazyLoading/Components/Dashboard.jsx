export default function Dashboard() {                 /* Here we are using the default, which indicate this file need to export only one main component, not more than one. */

  return <div>

            Dashboard page
  
         </div>

};