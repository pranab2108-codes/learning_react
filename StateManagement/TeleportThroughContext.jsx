import { createContext } from "react";


export const CountContext = createContext(0);               /* It returns an object which is a (dot).Provider, now we created the context with an initial value of 0. */